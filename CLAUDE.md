# 프로젝트 규칙

매번 말하지 않아도 항상 지켜야 하는 규칙입니다.

1. **파일 경로를 코드에 쓰지 않는다.** 사용자가 그때그때 업로드한 파일을 사용한다.
2. **엑셀·CSV 파일은 앱 폴더 밖에 둔다.** 그리고 `.gitignore`로 제외한다.
3. **API 키는 코드에 넣지 않는다.** `.env`에서 읽는다.
4. **답변은 항상 한국어로 한다.**
